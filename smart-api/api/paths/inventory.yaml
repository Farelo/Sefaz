paths:
  /inventory/general/{limit}/{page}:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Inventory"
        summary: "Inventory of the equipaments."
        description: ""
        operationId: geraneral_inventory_packing
        parameters:
        - in: "path"
          name: "limit"
          description: "The limit of packings that will be returned"
          required: true
          type: string
        - in: "path"
          name: "page"
          description: "The especific page that returns packings related a this page"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /inventory/battery/{limit}/{page}:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Inventory"
        summary: "Inventory of the equipaments Analyzing the battery."
        description: ""
        operationId: inventory_battery
        parameters:
        - in: "path"
          name: "limit"
          description: "The limit of packings that will be returned"
          required: true
          type: string
        - in: "path"
          name: "page"
          description: "The especific page that returns packings related a this page"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /inventory/permanence/{limit}/{page}/{code}:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Inventory"
        summary: "Inventory of the equipaments Analyzing the permanence."
        description: ""
        operationId: inventory_permanence
        parameters:
        - in: "path"
          name: "limit"
          description: "The limit of packings that will be returned"
          required: true
          type: string
        - in: "path"
          name: "page"
          description: "The especific page that returns packings related a this page"
          required: true
          type: string
        - in: "path"
          name: "code"
          description: "The especific page that returns packings related a this code"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /inventory/quantity/{limit}/{page}/{code}:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Inventory"
        summary: "Inventory of the equipaments Analyzing your quantity."
        description: ""
        operationId: quantity_inventory
        parameters:
        - in: "path"
          name: "limit"
          description: "The limit of packings that will be returned"
          required: true
          type: string
        - in: "path"
          name: "page"
          description: "The especific page that returns packings related a this page"
          required: true
          type: string
        - in: "path"
          name: "code"
          description: "The especific page that returns packings related a this code"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /inventory/packing/historic/{limit}/{page}/{serial}:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Inventory"
        summary: "Inventory of the equipaments Analyzing your historic."
        description: ""
        operationId: inventory_packing_historic
        parameters:
        - in: "path"
          name: "limit"
          description: "The limit of packings that will be returned"
          required: true
          type: string
        - in: "path"
          name: "page"
          description: "The especific page that returns packings related a this page"
          required: true
          type: string
        - in: "path"
          name: "serial"
          description: "The especific page that returns packings related a this code"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /inventory/battery/code/{limit}/{page}/{code}:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Inventory"
        summary: "Inventory of the equipaments Analyzing the battery by code."
        description: ""
        operationId: inventory_battery_by_code
        parameters:
        - in: "path"
          name: "limit"
          description: "The limit of packings that will be returned"
          required: true
          type: string
        - in: "path"
          name: "page"
          description: "The especific page that returns packings related a this page"
          required: true
          type: string
        - in: "path"
          name: "code"
          description: "The especific page that returns packings related a this code"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /inventory/supplier/{limit}/{page}/{supplier}:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Inventory"
        summary: "Inventory of the suppliers."
        description: ""
        operationId: supplier_inventory
        parameters:
        - in: "path"
          name: "limit"
          description: "The limit of packings that will be returned"
          required: true
          type: string
        - in: "path"
          name: "page"
          description: "The especific page that returns packings related a this page"
          required: true
          type: string
        - in: "path"
          name: "supplier"
          description: "The especific page that returns packings related a this supplier"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /inventory/plant/{limit}/{page}/{code}/{supplier}:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Inventory"
        summary: "Inventory of the equipaments by plant."
        description: ""
        operationId: geraneral_inventory_packing_by_plant
        parameters:
        - in: "path"
          name: "limit"
          description: "The limit of packings that will be returned"
          required: true
          type: string
        - in: "path"
          name: "page"
          description: "The especific page that returns packings related a this page"
          required: true
          type: string
        - in: "path"
          name: "code"
          description: "The especific page that returns packings related a this code"
          required: true
          type: string
        - in: "path"
          name: "supplier"
          description: "The especific page that returns packings related a this supplier"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
