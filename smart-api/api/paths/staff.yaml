paths:
#-------------------------------------------START CRUD STAFF  ----------------------------------
  /staff/retrieve/{staff_id}:
      x-swagger-router-controller: staff_controller
      get:
        tags:
          - "Staff"
        summary: "Get a staff on the system"
        description: ""
        operationId: staff_read
        parameters:
        - in: "path"
          name: "staff_id"
          description: "The code that needs to be fetched"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /staff/list/pagination/{limit}/{page}:
    x-swagger-router-controller: staff_controller
    get:
      tags:
        - "Staff"
      summary: "Get all staff on the system by pagination"
      description: ""
      operationId: staff_listPagination
      parameters:
      - in: "path"
        name: "limit"
        description: "The limit of staff that will be returned"
        required: true
        type: string
      - in: "path"
        name: "page"
        description: "The especific page that returns staff related a this page"
        required: true
        type: string
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /staff/create:
    x-swagger-router-controller: staff_controller
    post:
      tags:
      - "Staff"
      summary: "Add a new staff to the system"
      description: ""
      operationId: staff_create
      parameters:
      - in: "body"
        name: "body"
        description: "staff object that needs to be added to the system"
        required: true
        schema:
          $ref: "#/definitions/Staff"
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /staff/delete/{staff_id}:
    x-swagger-router-controller: staff_controller
    delete:
      tags:
      - "Staff"
      summary: "Remove a staff on the system"
      description: ""
      operationId: staff_delete
      parameters:
      - in: "path"
        name: "staff_id"
        description: "The code that needs to be fetched"
        required: true
        type: string
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /staff/update/{staff_id}:
    x-swagger-router-controller: staff_controller
    put:
      tags:
      - "Staff"
      summary: "Update a staff on the system"
      description: ""
      operationId: staff_update
      parameters:
      - in: "path"
        name: "staff_id"
        description: "The code that needs to be fetchede"
        required: true
        type: string
      - in: "body"
        name: "body"
        description: "The profile object that will to be modified"
        required: true
        schema:
          $ref: "#/definitions/Staff"
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
