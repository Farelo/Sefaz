paths:
#-------------------------------------------START CRUD SUPPLIER ----------------------------------
  /supplier/retrieve/{supplier_id}:
      x-swagger-router-controller: supplier_controller
      get:
        tags:
          - "Supplier"
        summary: "Get a supplier on the system"
        description: ""
        operationId: supplier_read
        security:
          - Bearer: []
        parameters:
        - in: "path"
          name: "supplier_id"
          description: "The code that needs to be fetched"
          required: true
          type: string
        responses:
          200:
            description: "Successfully"
            schema:
              $ref: "#/definitions/SuccessResponse"
          400:
            description: "Client Error"
            schema:
              $ref: "#/definitions/Error400"
          500:
            description: "Server Error"
            schema:
              $ref: "#/definitions/Error500"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /supplier/retrieve/duns/{supplier_duns}:
      x-swagger-router-controller: supplier_controller
      get:
        tags:
          - "Supplier"
        summary: "Get a supplier on the system"
        description: ""
        operationId: supplier_read_by_duns
        security:
          - Bearer: []
        parameters:
        - in: "path"
          name: "supplier_duns"
          description: "The code that needs to be fetched"
          required: true
          type: string
        responses:
          200:
            description: "Successfully"
            schema:
              $ref: "#/definitions/SuccessResponse"
          400:
            description: "Client Error"
            schema:
              $ref: "#/definitions/Error400"
          500:
            description: "Server Error"
            schema:
              $ref: "#/definitions/Error500"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /supplier/retrieve/duns/{duns}/name/{name}:
      x-swagger-router-controller: supplier_controller
      get:
        tags:
          - "Supplier"
        summary: "Get a supplier on the system by name and duns"
        description: ""
        operationId: supplier_read_by_dunsAndSupplier
        security:
          - Bearer: []
        parameters:
        - in: "path"
          name: "name"
          description: "The name that needs to be fetched"
          required: true
          type: string
        - in: "path"
          name: "duns"
          description: "The duns that needs to be fetched"
          required: true
          type: string
        responses:
          200:
            description: "Successfully"
            schema:
              $ref: "#/definitions/SuccessResponse"
          400:
            description: "Client Error"
            schema:
              $ref: "#/definitions/Error400"
          500:
            description: "Server Error"
            schema:
              $ref: "#/definitions/Error500"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /supplier/list/pagination/{limit}/{page}:
    x-swagger-router-controller: supplier_controller
    get:
      tags:
        - "Supplier"
      summary: "Get all suppliers on the system by pagination"
      description: ""
      operationId: supplier_list_pagination
      security:
        - Bearer: []
      parameters:
      - in: "path"
        name: "limit"
        description: "The limit of suppliers that will be returned"
        required: true
        type: string
      - in: "path"
        name: "page"
        description: "The especific page that returns suppliers related a this page"
        required: true
        type: string
      responses:
        200:
          description: "Successfully"
          schema:
            $ref: "#/definitions/SuccessResponse"
        400:
          description: "Client Error"
          schema:
            $ref: "#/definitions/Error400"
        500:
          description: "Server Error"
          schema:
            $ref: "#/definitions/Error500"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /supplier/list/all:
    x-swagger-router-controller: supplier_controller
    get:
      tags:
        - "Supplier"
      summary: "Get all suppliers on the system "
      description: ""
      operationId: supplier_list_all
      security:
        - Bearer: []
      responses:
        200:
          description: "Successfully"
          schema:
            $ref: "#/definitions/SuccessResponse"
        400:
          description: "Client Error"
          schema:
            $ref: "#/definitions/Error400"
        500:
          description: "Server Error"
          schema:
            $ref: "#/definitions/Error500"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /supplier/create:
    x-swagger-router-controller: supplier_controller
    post:
      tags:
      - "Supplier"
      summary: "Add a new supplier to the system"
      description: ""
      operationId: supplier_create
      security:
        - Bearer: []
      parameters:
      - in: "body"
        name: "body"
        description: "supplier object that needs to be added to the system"
        required: true
        schema:
          $ref: "#/definitions/Supplier"
      responses:
        200:
          description: "Successfully"
          schema:
            $ref: "#/definitions/SuccessResponse"
        400:
          description: "Client Error"
          schema:
            $ref: "#/definitions/Error400"
        500:
          description: "Server Error"
          schema:
            $ref: "#/definitions/Error500"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"


  /supplier/delete/{supplier_id}:
    x-swagger-router-controller: supplier_controller
    delete:
      tags:
      - "Supplier"
      summary: "Remove a supplier on the system"
      description: ""
      operationId: supplier_delete
      security:
        - Bearer: []
      parameters:
      - in: "path"
        name: "supplier_id"
        description: "The code that needs to be fetched"
        required: true
        type: string
      responses:
        200:
          description: "Successfully"
          schema:
            $ref: "#/definitions/SuccessResponse"
        400:
          description: "Client Error"
          schema:
            $ref: "#/definitions/Error400"
        500:
          description: "Server Error"
          schema:
            $ref: "#/definitions/Error500"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /supplier/update/{supplier_id}:
    x-swagger-router-controller: supplier_controller
    put:
      tags:
      - "Supplier"
      summary: "Update a supplier on the system"
      description: ""
      operationId: supplier_update
      security:
        - Bearer: []
      parameters:
      - in: "path"
        name: "supplier_id"
        description: "The code that needs to be fetchede"
        required: true
        type: string
      - in: "body"
        name: "body"
        description: "The project object that will to be modified"
        required: true
        schema:
          $ref: "#/definitions/Supplier"
      responses:
        200:
          description: "Successfully"
          schema:
            $ref: "#/definitions/SuccessResponse"
        400:
          description: "Client Error"
          schema:
            $ref: "#/definitions/Error400"
        500:
          description: "Server Error"
          schema:
            $ref: "#/definitions/Error500"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"


#-------------------------------------------FINISH CRUD SUPPLIER ----------------------------------
