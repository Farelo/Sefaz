
#-------------------------------------------START HISTORIC TAGS----------------------------------
  /historictags/retrieve/{historic_tags_id}:
      x-swagger-router-controller: historic_tags_controller
      get:
        tags:
          - "Historic Tags"
        summary: "Get a historic tags on the system"
        description: ""
        operationId: historic_tags_read
        parameters:
        - in: "path"
          name: "historic_tags_id"
          description: "The code that needs to be fetched"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /historictags/list/pagination/{limit}/{page}:
    x-swagger-router-controller: historic_tags_controller
    get:
      tags:
        - "Historic Tags"
      summary: "Get all historic tags on the system by pagination"
      description: ""
      operationId: historic_tags_listPagination
      parameters:
      - in: "path"
        name: "limit"
        description: "The limit of historic tags that will be returned"
        required: true
        type: string
      - in: "path"
        name: "page"
        description: "The especific page that returns historic tags related a this page"
        required: true
        type: string
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /historictags/list/all:
    x-swagger-router-controller: historic_tags_controller
    get:
      tags:
        - "Historic Tags"
      summary: "Get all historic tags on the system"
      description: ""
      operationId: historic_tags_list
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /historictags/create:
    x-swagger-router-controller: historic_tags_controller
    post:
      tags:
      - "Historic Tags"
      summary: "Add a new historic tags to the system"
      description: ""
      operationId: historic_tags_create
      parameters:
      - in: "body"
        name: "body"
        description: "Historic tags object that needs to be added to the system"
        required: true
        schema:
          $ref: "#/definitions/HistoricTags"
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"


  /historictags/delete/{historic_tags_id}:
    x-swagger-router-controller: historic_tags_controller
    delete:
      tags:
      - "Historic Tags"
      summary: "Remove a historic tags on the system"
      description: ""
      operationId: historic_tags_delete
      parameters:
      - in: "path"
        name: "historic_tags_id"
        description: "The code that needs to be fetched"
        required: true
        type: string
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /historictags/update/{historic_tags_id}:
    x-swagger-router-controller: historic_tags_controller
    put:
      tags:
      - "Historic Tags"
      summary: "Update a historic tags on the system"
      description: ""
      operationId: historic_tags_update
      parameters:
      - in: "path"
        name: "historic_tags_id"
        description: "The code that needs to be fetchede"
        required: true
        type: string
      - in: "body"
        name: "body"
        description: "The historic tags object that will to be modified"
        required: true
        schema:
          $ref: "#/definitions/HistoricTags"
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"

#-------------------------------------------FINISH CRUD HISTORIC TAGS----------------------------------
#-------------------------------------------START HISTORIC PACKINGS----------------------------------

#-------------------------------------------FINISH CRUD HISTORIC PACKINGS----------------------------------



#-------------------------------------------FINISH CRUD PROFILE ----------------------------------

#-------------------------------------------FINISH CRUD STAFF SUPPLIER  ----------------------------------

#-------------------------------------------FINISH CRUD STAFF  ----------------------------------
#-------------------------------------------START CRUD COMPANY ----------------------------------
  /company/retrieve/{company_id}:
      x-swagger-router-controller: company_controller
      get:
        tags:
          - "Company"
        summary: "Get a company on the system"
        description: ""
        operationId: company_read
        parameters:
        - in: "path"
          name: "company_id"
          description: "The code that needs to be fetched"
          required: true
          type: string
        responses:
          200:
            description: "Message"
            schema:
              $ref: "#/definitions/SuccessResponse"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"
  /company/list/pagination/{limit}/{page}:
    x-swagger-router-controller: company_controller
    get:
      tags:
        - "Company"
      summary: "Get all companies on the system by pagination"
      description: ""
      operationId: company_listPagination
      parameters:
      - in: "path"
        name: "limit"
        description: "The limit of companies that will be returned"
        required: true
        type: string
      - in: "path"
        name: "page"
        description: "The especific page that returns companies related a this page"
        required: true
        type: string
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /company/create:
    x-swagger-router-controller: company_controller
    post:
      tags:
      - "Company"
      summary: "Add a new company to the system"
      description: ""
      operationId: company_create
      parameters:
      - in: "body"
        name: "body"
        description: "company object that needs to be added to the system"
        required: true
        schema:
          $ref: "#/definitions/Company"
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"


  /company/delete/{company_id}:
    x-swagger-router-controller: company_controller
    delete:
      tags:
      - "Company"
      summary: "Remove a company on the system"
      description: ""
      operationId: company_delete
      parameters:
      - in: "path"
        name: "company_id"
        description: "The code that needs to be fetched"
        required: true
        type: string
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"
  /company/update/{company_id}:
    x-swagger-router-controller: company_controller
    put:
      tags:
      - "Company"
      summary: "Update a company on the system"
      description: ""
      operationId: company_update
      parameters:
      - in: "path"
        name: "company_id"
        description: "The code that needs to be fetchede"
        required: true
        type: string
      - in: "body"
        name: "body"
        description: "The company object that will to be modified"
        required: true
        schema:
          $ref: "#/definitions/Company"
      responses:
        200:
          description: "Message"
          schema:
            $ref: "#/definitions/SuccessResponse"
        default:
          description: "Error"
          schema:
            $ref: "#/definitions/ErrorResponse"

#-------------------------------------------FINISH CRUD COMPANY ----------------------------------


#-------------------------------------------FINISH CRUD ADMIN ----------------------------------

#-------------------------------------------FINISH CRUD ADMIN CLIENT----------------------------------

#-------------------------------------------FINISH CRUD LOGISTICOPERATOR ----------------------------------
