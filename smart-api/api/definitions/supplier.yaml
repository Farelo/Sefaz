definitions:
  Supplier:
    type: object
    properties:
      name:
        type: string
      duns:
        type: string
      profile:
        $ref: '#/definitions/Profile'
      plant:
        $ref: '#/definitions/Plant'
  SupplierUtil:
    type: object
    properties:
      name:
        type: string
      duns:
        type: string
  ArraySupplier:
    type : array
    items:
      type: string
