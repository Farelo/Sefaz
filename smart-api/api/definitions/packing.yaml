definitions:
  PackingHash:
    type: object
    properties:
      hashPacking:
        type: string
  Packing:
    type: object
    properties:
      code:
        type: string
      type:
        type: string
      weigth:
        type: number
        format: double
      width:
        type: number
        format: double
      heigth:
        type: number
        format: double
      length:
        type: number
        format: double
      capacity:
        type: integer
      tag:
        $ref: '#/definitions/Tag'
      code_tag:
        type: string
      supplier:
        $ref: '#/definitions/SupplierUtil'
      project:
        $ref: '#/definitions/Project'
      serial:
        type: string
