definitions:
  PackingHash:
    type: object
    properties:
      hashPacking:
        type: string
  ArrayPacking:
    type : array
    items:
      $ref: '#/definitions/Packing'
  Packing:
    type: object
    properties:
      code:
        type: string
      type:
        type: string
      weigth:
        type: "integer"
        format: "int64"
      width:
        type: "integer"
        format: "int64"
      heigth:
        type: "integer"
        format: "int64"
      length:
        type: "integer"
        format: "int64"
      capacity:
        type: "integer"
        format: "int64"
      tag:
        $ref: '#/definitions/Tag'
      code_tag:
        type: string
      supplier:
        $ref: '#/definitions/SupplierUtil'
      project:
        $ref: '#/definitions/Project'
      serial:
        type: string
