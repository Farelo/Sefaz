definitions:
  ArrayRoute:
    type : array
    items:
      $ref: '#/definitions/Route'
  Route:
    type: object
    properties:
      supplier:
        $ref: '#/definitions/SupplierUtil'
      plant_factory:
        $ref: '#/definitions/Plant'
      plant_supplier:
        $ref: '#/definitions/Plant'
      packing_code:
        type: string
