definitions:
  Department:
    type: object
    properties:
      name:
        type: string
      plant:
        $ref: '#/definitions/Plant'
      lat:
        type: number
        format: double
      lng:
        type: number
        format: double
    required:
      - name
      - lat
      - lng
      - plant
  ArrayDepartment:
    type : array
    items:
      $ref: '#/definitions/Department'
