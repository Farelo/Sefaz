definitions:
  Plant:
    type: object
    properties:
      plant_name:
        type: string
      lat:
        type: number
        format: double
      lng:
        type: number
        format: double
      location:
        type: string
    required:
      - plant_name
      - lat
      - lng
  ArrayPlant:
    type : array
    items:
      $ref: '#/definitions/Plant'
