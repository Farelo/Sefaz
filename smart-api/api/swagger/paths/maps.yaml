paths:
#-------------------------------------------START CRUD CEP----------------------------------
  /maps/packings:
      x-swagger-router-controller: packing_controller
      get:
        tags:
          - "Maps"
        summary: "Get all information to maps"
        description: ""
        operationId: find_all_packings
        security:
          - Bearer: []
        parameters:
        - in: "query"
          name: "family"
          description: "Search by family"
          required: false
          type: string
        - in: "query"
          name: "serial"
          description: "Search by serial"
          required: false
          type: string
        responses:
          200:
            description: "Successfully"
            schema:
              $ref: "#/definitions/SuccessResponse"
          400:
            description: "Client Error"
            schema:
              $ref: "#/definitions/Error400"
          500:
            description: "Server Error"
            schema:
              $ref: "#/definitions/Error500"
          default:
            description: "Error"
            schema:
              $ref: "#/definitions/ErrorResponse"